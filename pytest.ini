[pytest]
# Silence benign third-party deprecation noise (older matplotlib + newer
# pyparsing emit PyparsingDeprecationWarnings from matplotlib internals).
# This only affects test output — it does not change pipeline behavior.
filterwarnings =
    ignore::DeprecationWarning
